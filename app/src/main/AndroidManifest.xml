<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.android_cnc.acnc" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FileDialog"
            android:label="@string/title_activity_file_dialog" >
        </activity>
        <activity
            android:name=".GraphView.CNCControlActivity"
            android:label="@string/title_activity_cnccontrol" >
        </activity>
        <activity
            android:name=".GraphView.CNCControlViewActivity"
            android:label="@string/title_activity_cnccontrol_view"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ru.android_cnc.acnc.MainActivity" />
        </activity>

        <service
            android:name=".CNCService"
            android:enabled="true"
            android:exported="false" >
        </service>
        <service
            android:name=".THCService"
            android:exported="false" >
        </service>
    </application>

</manifest>
